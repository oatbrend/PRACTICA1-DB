services:
  db:
    image: postgres:18
    container_name: pg-practica1
    environment:
      POSTGRES_PASSWORD: practica1
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: